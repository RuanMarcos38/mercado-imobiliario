name: Apply property search quality fixes (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Fixes already applied
        run: echo "As correções de ranking e paginação já estão incorporadas ao código principal. Nenhuma alteração automática será executada."
