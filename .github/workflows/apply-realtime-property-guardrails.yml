name: Apply realtime property guardrails (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Guardrails already applied
        run: echo "Guardrails de frescor e contexto imobiliário já estão incorporados ao código principal. Nenhuma alteração automática será executada."
