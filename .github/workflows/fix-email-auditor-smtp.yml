name: Fix email auditor SMTP (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: SMTP fix already applied
        run: echo "A validação de SMTP Hostinger já está incorporada ao código principal. Nenhuma alteração automática será executada."
